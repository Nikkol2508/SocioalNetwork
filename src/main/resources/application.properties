
spring.profiles.active=dev-vitaly

spring.datasource.url=jdbc:${CLEARDB_DATABASE_URL:postgresql://localhost:5432/social_network_db}
spring.datasource.username=username
spring.datasource.password=password
spring.datasource.driver-class-name=org.postgresql.Driver
spring.datasource.sql-script-encoding= UTF-8
server.port=${PORT:8086}

spring.liquibase.enabled=true
spring.liquibase.change-log=classpath:/db/migrations/db.changelog-master.yaml

jwt.token.secret=socialnetwork
jwt.token.expired=15000